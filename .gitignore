# IDE
.idea/
*.iml
.vscode/

# Claude Code
.claude/settings.local.json
docs/.claude/

# MoAI (정본 산출물은 aaa/.moai 에 위치 — 이 레포에서 moai 실행 시 잘못 생기는 stray 무시)
.moai/

# OS
.DS_Store

# Environment & Secrets
.env
.env.*
!.env.example
# Alertmanager value-only token secrets (real files live in ${AAA_SSD_BASE}/secrets/, never committed)
alertmanager.bot_token
alertmanager.chat_id
!alertmanager.bot_token.example
!alertmanager.chat_id.example

# Docker
docker-compose.override.yml

# Review (Personal Notes)
docs/review/

# Temporary
*.tmp
*.bak
*.log
